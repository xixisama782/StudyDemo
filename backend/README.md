# GameCenter 后端

Spring Boot 3 + MyBatis-Plus + Redis + JWT。

**完整项目文档** → [../PROJECT.md](../PROJECT.md)

## 开发启动

```powershell
mvn spring-boot:run
```

默认 **dev** profile。环境变量模板见 `env.example`；部署见 PROJECT.md §8。
