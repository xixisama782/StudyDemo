# =============================================================================
# GameCenter 前端环境变量模板
# =============================================================================

# -----------------------------------------------------------------------------
# DEVELOPMENT（npm run dev）
# -----------------------------------------------------------------------------
# 通常无需 .env 文件：vite.config.ts 已将 /api、/uploads 代理到 http://localhost:8080
# 前端 dev 端口：10109（与 application-dev.yml 中 CORS 默认一致）

# 若需直连后端（跳过 Vite 代理）：
# VITE_API_BASE_URL=http://localhost:8080/api

# -----------------------------------------------------------------------------
# PRODUCTION（npm run build）
# -----------------------------------------------------------------------------
# 复制为 .env.production 或在 CI/CD 构建时注入

# 默认同源 Nginx 反代：/api → 后端 upstream
VITE_API_BASE_URL=/api
